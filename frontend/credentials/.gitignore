# Never commit signing keys / store credentials.
# Drop your Google Play service-account JSON here as:
#   play-store-service-account.json
# (referenced by eas.json → submit.production.android.serviceAccountKeyPath)
*
!.gitignore
